import { BrowserRouter, Routes, Route } from "react-router-dom";


const Router: React.FC = () => {
  return (
    <BrowserRouter>
      <Routes>


      </Routes>
    </BrowserRouter>
  );
};

export default Router;
